import React, { useEffect } from "react";
import AddPlayerForm from "../services/addPlayerForm";
import { useState } from "react";

function AddPlayer(props) { 

    useEffect(() => {
        
    })

    return(
        <div>
            <AddPlayerForm/>
        </div>
    );
}

export default AddPlayer;